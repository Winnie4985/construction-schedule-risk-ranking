# 核心資料處理與建模
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10

# 特徵工程(讀 Excel、依賴圖論)
openpyxl>=3.1
networkx>=3.0

# 模型解釋
shap>=0.44

# 視覺化
matplotlib>=3.7

# 互動式 Demo(app.py)
streamlit>=1.30

# 若要重跑 requirements 之外的探索(如 experiments/algorithm_comparison.py 的
# HistGradientBoostingClassifier)不需要額外套件,scikit-learn 已內建。
